{
  "version": 2,
  "buildCommand": "npm run build -w @excel-ai-platform/renderer",
  "outputDirectory": "apps/renderer/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "^/(?!api).*", "dest": "/index.html" }
  ]
}
